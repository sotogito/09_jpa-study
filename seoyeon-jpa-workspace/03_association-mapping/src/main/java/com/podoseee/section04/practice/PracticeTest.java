package com.podoseee.section04.practice;

public class PracticeTest {

    /*
        예제.
        한 팀에는 여러 회원들이 존재하며
            1           M
        각 회원들은 각각 하나의 라커를 가지고 있다.
            1                     1

        < Team >        < Member >          < Locker >
        team_id(pk)     member_id(pk)       locker_id(pk)
        team_name       member_name         locker_name
                        team_id(fk)
                        locker_id(fk)

     */

}